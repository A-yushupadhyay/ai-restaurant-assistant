fastapi
uvicorn[standard]
pydantic
python-dotenv
python-multipart
boto3
openai==0.28.0

# Optional (OCR): install these to enable image/PDF menu OCR
# Note: `pdf2image` requires the `poppler` system package (install via
# your OS package manager). `pytesseract` requires the Tesseract binary.
Pillow
pytesseract
pdf2image

